# Source Verification Matrix

Status: Sprint 2 live public market-data verification.

Verification date: 2026-06-07.

Scope: public unauthenticated spot market data only. Perp, private exchange calls, and news sources remain `TO_VERIFY`.

| Source | Endpoint | Official URL | Verified Date | Auth | Secret | Rate Limit | Params | Response Shape | Internal Mapping | Fallback | Fixture | Prod Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Binance Spot | `GET /api/v3/klines` on `https://data-api.binance.vision` | https://developers.binance.com/docs/binance-spot-api-docs/rest-api/market-data-endpoints | 2026-06-07 | No | No | Public request weight; local throttle configured by `UCPE_PROVIDER_RATE_LIMIT_PER_MIN` | `symbol`, `interval`, `limit` max `1000` | Rows ascending: `[openTime, open, high, low, close, volume, closeTime, ...]`; last row may be in-progress | `BTC/USDT -> BTCUSDT`; `15m,1H,4H,1D,1W,1M -> 15m,1h,4h,1d,1w,1M`; drop last row; set close time as open plus interval | No silent fallback; provider failure is visible | Explicit `UCPE_DATA_MODE=fixture` only | `VERIFIED_PUBLIC` / `REQUIRED_PUBLIC` |
| Binance Spot | `GET /api/v3/depth` on `https://data-api.binance.vision` | https://developers.binance.com/docs/binance-spot-api-docs/rest-api/market-data-endpoints | 2026-06-07 | No | No | Public request weight; local throttle configured by `UCPE_PROVIDER_RATE_LIMIT_PER_MIN` | `symbol`, `limit=100` | `bids` and `asks` arrays of `[price, qty]` | Normalize to internal order-book levels; crossed/locked books fail closed | No silent fallback; provider failure is visible | Explicit `UCPE_DATA_MODE=fixture` only | `VERIFIED_PUBLIC` / `REQUIRED_PUBLIC` |
| Binance Spot | `GET /api/v3/exchangeInfo`, `GET /api/v3/time` on `https://data-api.binance.vision` | https://developers.binance.com/docs/binance-spot-api-docs/rest-api/market-data-endpoints | 2026-06-07 | No | No | Public request weight; optional validation only | Optional symbol/time validation params | Exchange metadata and server time | Optional validation endpoints; not required for Sprint 2 hot path | No silent fallback | Explicit `UCPE_DATA_MODE=fixture` only | `VERIFIED_PUBLIC` / `OPTIONAL_PUBLIC` |
| OKX Spot | `GET /api/v5/market/candles` on `https://www.okx.com` | https://www.okx.com/docs-v5/en/ | 2026-06-07 | No | No | Public IP-based limits; local throttle configured by `UCPE_PROVIDER_RATE_LIMIT_PER_MIN` | `instId`, `bar`, `limit` max `300` | Rows newest first: `[ts, o, h, l, c, vol, volCcy, volCcyQuote, confirm]`; `confirm="0"` is in-progress | `BTC/USDT -> BTC-USDT`; `15m,1H,4H,1D,1W,1M -> 15m,1H,4H,1Dutc,1Wutc,1Mutc`; UTC candle mappings reduce Binance/OKX boundary mismatch; drop unconfirmed rows; reverse ascending; set close time as open plus interval | No silent fallback; provider failure is visible. If cross-provider confirmation is optional and providers disagree, use explicit single public-provider live fallback with provider-state warning. | Explicit `UCPE_DATA_MODE=fixture` only | `VERIFIED_PUBLIC` / `REQUIRED_PUBLIC` |
| OKX Spot | `GET /api/v5/market/books` on `https://www.okx.com` | https://www.okx.com/docs-v5/en/ | 2026-06-07 | No | No | Public IP-based limits; local throttle configured by `UCPE_PROVIDER_RATE_LIMIT_PER_MIN` | `instId`, `sz=100` | `bids` and `asks` arrays of `[price, size, liqOrders, numOrders]` | Normalize to internal order-book levels; crossed/locked books fail closed | No silent fallback; provider failure is visible | Explicit `UCPE_DATA_MODE=fixture` only | `VERIFIED_PUBLIC` / `REQUIRED_PUBLIC` |
| OKX Spot | `GET /api/v5/public/instruments?instType=SPOT` on `https://www.okx.com` | https://www.okx.com/docs-v5/en/ | 2026-06-07 | No | No | Public IP-based limits; optional validation only | `instType=SPOT` | Instrument metadata | Optional symbol validation endpoint; not required for Sprint 2 hot path | No silent fallback | Explicit `UCPE_DATA_MODE=fixture` only | `VERIFIED_PUBLIC` / `OPTIONAL_PUBLIC` |
| Perp venues | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `CRYPTO_PERP` remains gated and isolated | Disabled unless future Claude/User approval | Not applicable | `TO_VERIFY` |
| News sources | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | `TO_VERIFY` | Metadata/snippet only; no full bodies | `METRICS_ONLY` fetches no news; Sprint 2 does not add live news | News failure never crashes metrics | News fixtures only | `TO_VERIFY` |
| Supabase REST/PostgREST | HTTPS REST API under `SUPABASE_URL` | https://supabase.com/docs | 2026-06-08 | Yes | Yes, backend service role secret only | Project/API limits apply | HTTPS `443`; tables via `/rest/v1/...` | Compact rows only: watchlist, run summaries, timeframe summaries, provider observations, app events | `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` selects `SUPABASE_REST`; preferred for Hugging Face runtime | Persistence failure reports `UNAVAILABLE` while analysis returns | Mocked `httpx` in tests; in-memory fallback for outage/stateless mode | `WAVE1_2_HF_RUNTIME_PERSISTENCE` |
| Supabase Postgres | Direct Postgres URL for app persistence/migrations | https://supabase.com/docs | 2026-06-07 | Yes | Yes, backend secret only | Project/database limits apply | SQL over Postgres connection, commonly `5432`/`6543` | Compact rows only: watchlist, run summaries, timeframe summaries, provider observations, app events | `SUPABASE_DB_URL` selects `SUPABASE_POSTGRES` only when REST secrets are absent; still used by migration script | No analysis fallback needed; persistence failure reports `UNAVAILABLE` while analysis returns | In-memory repository for tests/stateless mode | `WAVE1_OPTIONAL_LOCAL_OR_NON_HF_PERSISTENCE` |
